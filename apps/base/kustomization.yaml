apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kite
  - playground
  - l2-announcements
  - ingress
  - radar
  # weave-gitops removed — Helm repo (https://helm.gitops.weave.works) is
  # behind Cloudflare and ghcr.io OCI returns 401 (private).
  # Re-add if you have credentials: apps/base/weave-gitops/
