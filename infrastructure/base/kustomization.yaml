apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cilium
  - longhorn
  - monitoring
  - gateway-api
  - metrics-server
  # keycloak removed: managed by ArgoCD via auction-platform GitLab repo
  # (infrastructure/base/keycloak/ was an old dev deployment conflicting
  #  with the ArgoCD-managed Keycloak in the same namespace)
